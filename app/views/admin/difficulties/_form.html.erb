<%= form_for(@admin_difficulty) do |f| %>
  <% if @admin_difficulty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_difficulty.errors.count, "error") %> prohibited this admin_difficulty from being saved:</h2>

      <ul>
      <% @admin_difficulty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :points %><br>
    <%= f.number_field :points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
